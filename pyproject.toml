[project]
name = "private-credit-work"
version = "0.1.0"
description = "Cliffwater private credit liquidity analysis and extraction tooling"
requires-python = ">=3.10"
dependencies = [
  "pdfminer.six>=20221105",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
